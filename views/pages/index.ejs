<!DOCTYPE html>
<html lang="es">
    <head>
        <%- include('../secciones/head'); %>
    </head>
    <body class="container">
        
        <header>
            <%- include('../secciones/menu'); %>
        </header>
        
        <main>
            <div class="jumbotron">
                <h1>Ejemplo EJS</h1>
                <p>Universidad Tecnológica de San Juan del Río</p>
                <h2><%= titulo %></h2>
                <ul>
                    <% heroes.forEach(function(heroe) { %>
                        
                        <li>
                            <strong><%= heroe.nombre %></strong> cuya identidad es <%= heroe.identidad %> 
                            y es publicado por <%= heroe.empresa %>
                        </li>

                    <% }); %>
                </ul>
            </div>
        </main>

        <footer>
            <%- include('../secciones/footer'); %>
        </footer>
    </body>
</html>